import { DocumentDataSource } from './document-data-source';

describe('DocumentDataSource', () => {
  it('should create an instance', () => {
    expect(new DocumentDataSource()).toBeTruthy();
  });
});
